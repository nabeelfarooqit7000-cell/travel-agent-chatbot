services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      HOST: "0.0.0.0"
      PORT: "8000"
    volumes:
      # Persist knowledge JSON + backups across container restarts
      - ./app/data:/app/app/data
